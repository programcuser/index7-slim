<?php if (isset($flash['success'])) : ?>
  <?php foreach ($flash['success'] as $mess): ?>
    <div> <p> <?= $mess ?> </p> </div>
  <?php endforeach ?>
<?php endif ?>

<?php if (count($users) !== 0) : ?>
<table>
  <?php foreach ($users as $user): ?>
    <tr>
      <td>
          <?= htmlspecialchars($user['nickname']) ?>
      </td>
    </tr>
  <?php endforeach ?>
</table>
<?php endif ?>